# SQLite (local development — zero setup required)
DATABASE_URL="file:./dev.db"

# Wedding ID — set after first seed run (check console output)
# The seed script will print the wedding ID
WEDDING_ID="vivah-2026"

# Switch to PostgreSQL for production:
# DATABASE_URL="postgresql://user:password@host:5432/vivah"
